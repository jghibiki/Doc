'use strict';

describe('doc.client module', function() {

  beforeEach(module('doc.client'));

  describe('client controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var clientCtrl = $controller('ClientCtrl');
      expect(clientCtrl).toBeDefined();
    }));

  });
});